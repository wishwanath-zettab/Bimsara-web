{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
  "start": "concurrently \"npm run backend\" \"npm run frontend\"",
  "backend": "cd backend && npm install && npm start",
  "frontend": "cd frontend && npm install && npm run build && serve -s build"
}
}
