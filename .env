JWT_SECRET=change-me-to-a-strong-random-secret
ADMIN_PASSWORD=admin

# Host port published on all interfaces; maps 1:1 to the container's PORT
# (8100). The container speaks plain HTTP — nginx terminates TLS in front of it
# and proxies to http://127.0.0.1:8100.
HTTP_HOST_PORT=8100
